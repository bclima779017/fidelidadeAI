# ── API Gemini ──
GEMINI_API_KEY=

# ── CORS (origens permitidas, separadas por vírgula) ──
CORS_ORIGINS=http://localhost:3000

# ── Rate Limiting ──
RATE_LIMIT_REQUESTS=10
RATE_LIMIT_WINDOW=minute

# ── Frontend (variável de build Next.js) ──
NEXT_PUBLIC_API_URL=http://localhost:8000
